/**
 * validation.js - Centralized validation logic
 * 
 * Rules:
 * - Perform client-side validation only once when the user submits a form.
 * - Do not show errors during typing to avoid annoying the user.
 */

export const validateForm = (data) => {
  // TODO: Add validation logic
  return { isValid: true, errors: {} };
};
